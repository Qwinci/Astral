name=xtrans
revision=1
from_source=xtrans
imagedeps="meson ninja-build gcc build-essential"
hostdeps="xgcc xbinutils pkgconfig"
deps="base"

configure() {
	${source_dir}/configure --prefix=${prefix}
}

build() {
	make -j${parallelism}
}

package() {
	DESTDIR=${dest_dir} make install
}
