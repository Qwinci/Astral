name=libtool
revision=1
from_source=libtool
hostdeps="autoconf automake"
imagedeps="gcc build-essential help2man"

configure() {
	cp -pr ${source_dir}/* .
	./configure --prefix=${prefix}
}

build() {
	make -j${parallelism}
}

package() {
	make install DESTDIR="${dest_dir}"
}

