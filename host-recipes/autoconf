name=autoconf
revision=1
from_source=autoconf
imagedeps="gcc"

configure() {
	${source_dir}/configure --prefix=${prefix}
}

build() {
	make -j${parallelism}
}

package() {
	make install DESTDIR="${dest_dir}"
}
