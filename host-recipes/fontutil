name=fontutil
revision=1
from_source=fontutil
imagedeps="gcc build-essential"
hostdeps="xgcc xbinutils pkgconfig util-macros xorgproto"

configure() {
	${source_dir}/configure --prefix=${prefix}
}

build() {
	make -j${parallelism}
}

package() {

	make install DESTDIR=${dest_dir}

}
