name=python
from_source=python
revision=1
imagedeps="build-essential"

configure() {
	"${source_dir}/configure" --prefix=${prefix}
}

build() {
	make -j${parallelism}
}

package() {
	make install DESTDIR=${dest_dir}
}
